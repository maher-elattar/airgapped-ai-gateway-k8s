repos:
  - repo: local
    hooks:
      - id: portfolio-secret-boundary
        name: portfolio secret boundary
        entry: scripts/secret-scan.sh --staged
        language: system
        pass_filenames: false
