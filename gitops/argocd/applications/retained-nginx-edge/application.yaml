apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ai-gateway-retained-nginx-edge
  namespace: argocd
  labels:
    app.kubernetes.io/name: ai-gateway
    app.kubernetes.io/component: gitops-application
    app.kubernetes.io/part-of: airgap-ai-gateway
    airgap.ai.gateway/baseline: v1.3.1
    airgap.ai.gateway/profile: retained-nginx-edge
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  project: airgap-ai-gateway
  source:
    repoURL: https://github.com/ahmed658/airgapped-ai-gateway-k8s.git
    targetRevision: main
    path: gitops/argocd/managed-overlays/retained-nginx-edge
  destination:
    server: https://kubernetes.default.svc
    namespace: ai-gateway
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - ApplyOutOfSyncOnly=true
      - PruneLast=true
      - FailOnSharedResource=true
      - RespectIgnoreDifferences=true
    retry:
      limit: 3
      backoff:
        duration: 20s
        factor: 2
        maxDuration: 3m
  revisionHistoryLimit: 10
